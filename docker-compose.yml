services:
  randkapl:
    build: .
    container_name: randkapl-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/database.db
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
